# 🖥️ 로컬 스크래퍼 & 모바일 뷰 설정 가이드

네이버의 자동화 차단 정책을 피해, 내 컴퓨터에서 직접 스크래퍼를 돌리고 결과를 폰으로 확인하는 방법입니다.

## 1. 초기 브라우저 설치 (한 번만 수행)

이미 Playwright 라이브러리가 설치되어 있으므로, 실제 브라우저 엔진만 한 번 더 설치해 줍니다.
터미널에서 아래 명령어를 입력하세요:

```powershell
# Playwright 브라우저 설치
python -m playwright install chromium
```

## 2. 수동 실행 및 확인

수정이 완료된 데이터가 GitHub에 잘 올라가는지 테스트해 봅니다:

```powershell
./sync_listings.ps1
```

정상적으로 완료되면, 잠시 후 본인의 **GitHub Pages 대시보드 주소**로 접속하여 데이터가 갱신되었는지 확인하세요.

## 3. 윈도우 작업 스케줄러 자동화 (추천)

컴퓨터가 켜져 있을 때 매일 자동으로 실행되도록 설정하는 방법입니다.

1.  **시작** 메뉴에서 `작업 스케줄러` 검색 및 실행
2.  오른쪽 **기본 작업 만들기** 클릭
3.  **이름**: `MyBudongsan_Scraper` 입력
4.  **트리거**: 매일 (원하는 시간 설정, 예: 오전 10시)
5.  **작업**: 프로그램 시작
6.  **프로그램/스크립트**: `powershell.exe` 입력
7.  **인수 추가(옵션)**: `-ExecutionPolicy Bypass -File "D:\8. Antigravity\myBudongsan\sync_listings.ps1"`
    *   *주의: 경로가 다를 경우 실제 경로로 수정해 주세요.*
8.  **시작 위치(옵션)**: `D:\8. Antigravity\myBudongsan`
9.  **마침** 클릭

이제 컴퓨터가 켜져 있는 시간대라면 지정된 시간에 자동으로 스크래핑을 하고 GitHub로 데이터를 밀어넣습니다.

## 4. 📱 폰으로 확인하기

1.  GitHub Pages URL을 스마트폰 브라우저에서 즐겨찾기 해두세요.
2.  이동 중이거나 밖에서도 언제든지 최신 매물 현황과 변동 사항(신규, 가격 변동, 거래 완료)을 확인하실 수 있습니다.
